<style>
    #openProduct{
        width:60%;
        border:1px solid green;
}
</style>

<section id="order">
    <div id="openProduct">
        <div class="openclass">кнопка 1</div>
        <div class="closeclass">кнопка 1</div>
        <div class="openclass">кнопка 2</div>
        <div class="closeclass">кнопка 2</div>
    </div>
    <div id="deferred product">
        <div id="bbb">0</div>
        <div id="aaa"></div>
    </div>



</section>

<script>



    let openclass = document.querySelectorAll('.openclass')
    let closeclass = document.querySelectorAll('.closeclass')
    let xxx = document.querySelectorAll('.xxx')




    // Добавляет товар в карзину начало
    for (let i = 0; i <= 1; i++) {


        openclass[i].onclick = function () {

            localStorage.setItem(openclass[i].innerHTML, 'Y');
            // Обновление страницы начало
            window.location.reload(true);
            // Обновление страницы конец

        }
    }
    // Добавляет товар в карзину конец

    // Удаляет товар из карзины начало
    for (let i = 0; i <= 1; i++) {

        closeclass[i].onclick = function () {

            localStorage.removeItem(closeclass[i].innerHTML, 'Y');
            // Обновление страницы начало
            window.location.reload(true);
            // Обновление страницы конец
        }

    }
    // Удаляет товар из карзины конец
    s = 0 // Количество выбранных товаров
    for (const [key, value] of Object.entries(localStorage)) {
        // Количество выбранных товаров начало
        if (key.length = true) {
            s++
        }

        bbb.innerHTML = s
        // Количество выбранных товаров конец
        aaa.innerHTML += key //+ value
    }

    // Обнавляет страницу начало
    menu.onclick = function (event) {
        if (event.target.nodeName != 'A') return;

        let href = event.target.getAttribute('href');
        alert(href); // может быть подгрузка с сервера, генерация интерфейса и т.п.

        return false; // отменить действие браузера (переход по ссылке)
    };
    // Обнавляет страницу конец



</script>